# Version control
.git
.gitignore

# Python caches / virtual envs
.venv
venv
env
__pycache__
*.py[cod]
.pytest_cache

# Secrets & local data — NEVER ship these
.env
*.db
*.sqlite3

# Generated / local artifacts
outputs

# Notebooks (kept in the repo for reference, not needed at runtime)
Notebooks

# Local planning / working notes + package docs (not needed at runtime)
memory.md
implementation_plan.md
db/README.md

# Editor / OS cruft
.vscode
.DS_Store

# Docker files themselves (not needed inside the image)
Dockerfile
docker-compose.yml
.dockerignore
